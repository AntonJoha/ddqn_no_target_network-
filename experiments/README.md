# LunarLander experiments

This folder groups the two requested LunarLander runs:

- `tgelu_dqn_drop_target_network/` — DQN-style run with TGeLU that drops the target network
- `ddqn_no_tgelu_full_run/` — DDQN full run without TGeLU
